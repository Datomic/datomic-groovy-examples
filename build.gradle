apply plugin: 'eclipse'
apply plugin: 'groovy'

repositories {
    mavenCentral()
    mavenLocal()
    maven {
        url "http://clojars.org/repo"
    }
}

dependencies {
    compile 'com.datomic:datomic-free:0.8.4007'
}

task(classpath) {
    println sourceSets.main.runtimeClasspath.asPath
}
